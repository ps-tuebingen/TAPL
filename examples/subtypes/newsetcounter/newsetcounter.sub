\_:Unit.(
  let (r = {x=ref(1)}) in 
  (\r:{x:Ref(Nat)}.(
    fix (
      \self:{ get : (unit -> nat), set: (nat->unit), inc:(unit->unit) }.({
        get: \_:Unit.(!(r.x)),
        set: \i:Nat.(!(r.x) := i),
        inc: \_:Unit.((self.set)(succ(self.get(unit))))
    })
  )))(r)
)
