fix 
  (\ieio:{iseven:(Nat -> Bool), isodd:(Nat -> Bool)}. 
   ({iseven = 
      \x:Nat.(if (iszero(x)) { 
        true 
      } else { 
        (ieio.isodd)(pred(x)) 
      }),
    isodd = \x:Nat.(if (iszero(x)) { 
        false 
      } else { 
      (ieio.iseven) (pred(x)) 
      })
    }))
